# Air Quality Intelligence — Python dependencies
# Pin to known-good major versions for reproducibility.

# Core DB access layer
SQLAlchemy>=2.0,<3.0
GeoAlchemy2>=0.14,<1.0
# Binary wheels only — no local libpq build step needed on Windows.
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0

# Task 2 — spike detection pipeline: HTTP for OpenWeatherMap client.
requests>=2.31,<3.0

# Task 3 — attribution pipeline: geometry decoding from PostGIS WKB blobs.
shapely>=2.0,<3.0

# Task 4 — REST API layer
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0

# Phase 1 — Live Data Pipelines & Configuration
apscheduler>=3.10,<4.0
pyyaml>=6.0,<7.0
